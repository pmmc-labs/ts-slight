


(let lst-of-100 (list
    1 2 3 4 5
))

(defun fold (init f seq)
    (if (nil? seq) init
        (f (car seq) (fold init f (cdr seq)))))

(time-it "list - map")
(let result (fold/r () (lambda (x acc) (cons (inc x) acc)) lst-of-100))
(time-it/end "list - map")

(pprint result)


