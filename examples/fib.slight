(defun fib (n)
    (if (< n 2) n
        (+ (fib (- n 1)) (fib (- n 2)))))

(time-it "fib")
(sys/io/print-ln "fib(27) = " (fib 27))
(time-it/end "fib")

;; -----------------------------------------------------------------------------
;; Timings (fib 27, compiled js, this machine)
;; -----------------------------------------------------------------------------
;; before env refactor : 970.682ms
;; after  env refactor : <fill in from Task 4>
;; -----------------------------------------------------------------------------
